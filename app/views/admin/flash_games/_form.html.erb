<%= form_for(@admin_flash_game) do |f| %>
  <% if @admin_flash_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_flash_game.errors.count, "error") %> prohibited this admin_flash_game from being saved:</h2>

      <ul>
      <% @admin_flash_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
